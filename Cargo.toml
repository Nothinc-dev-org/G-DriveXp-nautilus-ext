[package]
name = "gdrivexp-nautilus"
version = "1.1.0"
edition = "2024"
license = "GPL-3.0-only"

[lib]
crate-type = ["cdylib"]

[dependencies]
# Serialización IPC
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
bincode = "1.3"

# Async runtime (solo para cliente IPC)
tokio = { version = "1.44", features = ["rt", "io-util", "net", "time"] }

# GLib / GObject types
glib-sys = "0.20"
gobject-sys = "0.20"
gio-sys = "0.20"

# Utilidades
# Utilidades
libc = "0.2"
percent-encoding = "2.3"
crossbeam-channel = "0.5"

[build-dependencies]
pkg-config = "0.3"
